import json
import requests
import time
from pathlib import Path

# 캐시 방지를 위한 파라미터 추가
ALL_JSON_URL = f"https://smok95.github.io/lotto/results/all.json?t={int(time.time())}"
LOCAL_FILE = Path("2025lotto_numbers_1_to_1182_final.json")

def main():
    if not LOCAL_FILE.exists():
        print(f"❌ 로컬 파일을 찾을 수 없습니다: {LOCAL_FILE}")
        return

    # 브라우저인 것처럼 속이는 헤더 (차단 방지 핵심)
    headers = {
        'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
    }

    try:
        print(f"🌐 데이터 가져오는 중: {ALL_JSON_URL}")
        res = requests.get(ALL_JSON_URL, headers=headers, timeout=20)
        res.raise_for_status()
        
        # JSON 응답인지 검증
        if "application/json" not in res.headers.get("Content-Type", "").lower():
            print("⚠️ 경고: 응답이 JSON 형식이 아닙니다. (HTML 에러 페이지일 가능성)")
            return
            
        all_data = res.json()
    except Exception as e:
        print(f"❌ 데이터 수집 실패: {e}")
        return

    with open(LOCAL_FILE, "r", encoding="utf-8") as f:
        local_data = json.load(f)

    local_latest = local_data[0]["draw_no"]
    latest_item = all_data[-1]
    latest_draw = latest_item["draw_no"]

    print(f"📌 현재 로컬: {local_latest}회 / GitHub 최신: {latest_draw}회")

    if latest_draw > local_latest:
        new_entry = {
            "draw_no": latest_draw,
            "numbers": latest_item["numbers"],
            "bonus": latest_item["bonus_no"] # 원본의 bonus_no를 bonus로 변환
        }
        local_data.insert(0, new_entry)
        
        with open(LOCAL_FILE, "w", encoding="utf-8") as f:
            json.dump(local_data, f, ensure_ascii=False, indent=2)
        print(f"🎉 {latest_draw}회차 업데이트 완료!")
    else:
        print("✅ 이미 최신 상태입니다.")

if __name__ == "__main__":
    main()
