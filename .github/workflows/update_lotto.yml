import json
import requests
from pathlib import Path

ALL_JSON_URL = "https://smok95.github.io/lotto/results/all.json"
LOCAL_FILE = Path("2025lotto_numbers_1_to_1182_final.json")

def main():
    # 1️⃣ 로컬 JSON 로드
    if not LOCAL_FILE.exists():
        print("❌ 로컬 JSON 파일 없음")
        return

    with open(LOCAL_FILE, "r", encoding="utf-8") as f:
        local_data = json.load(f)

    local_latest = max(item["draw_no"] for item in local_data)
    print(f"📌 로컬 최신 회차: {local_latest}")

    # 2️⃣ 전체 회차 JSON 로드
    res = requests.get(ALL_JSON_URL, timeout=15)
    res.raise_for_status()
    all_data = res.json()

    if not all_data:
        print("❌ all.json 비어 있음")
        return

    # 3️⃣ 최신 회차 = 맨 마지막
    latest = all_data[-1]
    latest_draw = latest["draw_no"]

    print(f"🌐 미러 최신 회차: {latest_draw}")

    if latest_draw <= local_latest:
        print("✅ 이미 최신 상태")
        return

    # 4️⃣ 포맷 변환
    new_entry = {
        "draw_no": latest_draw,
        "numbers": latest["numbers"],
        "bonus": latest["bonus_no"]
    }

    # 5️⃣ 맨 앞에 추가
    local_data.insert(0, new_entry)

    # 6️⃣ 저장
    with open(LOCAL_FILE, "w", encoding="utf-8") as f:
        json.dump(local_data, f, ensure_ascii=False, indent=2)

    print(f"🎉 {latest_draw}회차 업데이트 완료")

if __name__ == "__main__":
    main()
